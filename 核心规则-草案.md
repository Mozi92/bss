# 附件二：33复制模式推广细则

> **文档状态**：草案（Draft）
> **版本**：v0.0.1
> **编制人**：张牧之
> **编制日期**：2026年
> **关联文档**：《膳食类产品线上推广平台合作方案》


## 1. 模式概述

33复制模式是一种基于三级裂变的推广激励机制，核心逻辑为：**每位推广者最多直接发展3位直属下级**，通过直返（百分比）与间返（固定数额）双重激励机制，推动推广者积极发展新用户并持续产生销售。

该模式强调 **“简单复制、快速裂变”**，充分利用运营方现有的人脉资源实现快速扩张。


## 2. 核心概念定义

| 术语 | 定义 |
|------|------|
| **推广者** | 参与推广体系的注册用户，可发展下级并获取返积分 |
| **直属下级** | 由某推广者直接推荐发展的用户，每位推广者最多3位 |
| **直接发展人** | 推荐并发展当前用户的推广者，即“谁推荐的谁” |
| **挂靠上级** | 当前用户在推广体系中的上一层级节点（可能非直接发展人） |
| **直返** | 直接发展人从下级订单中按百分比获得的返积分 |
| **间返** | 非直接发展人的层级节点按固定数额获得的返积分 |
| **滑落** | 当某推广者名下已有3位直属下级时，第4位新用户自动挂靠到其下级名下 |
| **顶层节点** | 推广体系最顶端的账号，通常为运营方指定 |


## 3. 层级结构规则

### 3.1 基本结构

```
        顶层节点
        /   |   \
    第1位  第2位  第3位     ← 每位推广者最多3位直属下级
     /|\    /|\    /|\
    ...    ...    ...       ← 下级同样各自发展3位
```

### 3.2 发展规则

| 规则项 | 说明 |
|--------|------|
| 直属下级上限 | 每位推广者最多 **3位** 直属下级 |
| 第4位处理 | 自动滑落到已有下级名下（按系统规则分配） |
| 滑落顺序 | 可按注册时间、活跃度或系统算法自动分配 |
| 层级深度 | 理论不限，实际受返积分层级限制 |
| 发展关系 | 一旦建立，**不可变更**（退款/注销等特殊情况除外） |
| 挂靠关系 | 可随滑落规则动态调整 |


## 4. 返积分体系

### 4.1 积分基本规则

| 项目 | 说明 |
|------|------|
| 积分与人民币兑换 | 1积分 = 1元人民币 |
| 积分来源 | 仅通过推广返利获得，不可充值购买 |
| 积分有效期 | 12个月（自获得之日起），过期自动清零 |
| 提现方式 | 申请兑换，乙方审核后线下转账 |
| 最低提现额度 | 100积分 |
| 提现频率 | 每月最多2次 |


### 4.2 返积分类型

| 类型 | 计算方式 | 计算基础 | 返利对象 |
|------|----------|----------|----------|
| **直返** | 按销售额百分比 | **按订单（每单）** | 始终为直接发展人 |
| **间返** | 固定数额 | **按件数（每件）** | 非直接发展人的层级节点 |


## 5. 直返规则（百分比制 · 按单计算）

### 5.1 直返比例分配

每位推广者名下3位直属下级的直返比例 **可不同**，通过差异化比例激励推广积极性。

| 直属下级序号 | 直返比例 | 说明 |
|-------------|----------|------|
| 第1位直属下级 | **20%** | 该下级每笔订单销售额的20%返给发展人 |
| 第2位直属下级 | **20%** | 该下级每笔订单销售额的20%返给发展人 |
| 第3位直属下级 | **60%** | 该下级每笔订单销售额的60%返给发展人 |

> **激励逻辑**：第3位直属下级的直返比例高达60%，激励推广者积极发展第3位下级，形成完整的33复制结构。

### 5.2 直返核心规则

| 规则项 | 说明 |
|--------|------|
| 计算基础 | **按订单（每单）** 计算，与订单内产品件数无关 |
| 返利对象 | **始终返给当前用户的直接发展人** |
| 返利比例 | 根据该下级在发展人名下的序号确定（20%/20%/60%） |
| 是否受挂靠影响 | **否**，直返不受挂靠层级影响，只看“谁发展的” |

### 5.3 直返计算示例

```
场景：
- 推广者A发展了3位直属下级：B1（第1位）、B2（第2位）、B3（第3位）
- 产品单价：100元/件

示例1：B1下单购买1件（订单金额100元）
→ A获得直返：100 × 20% = 20积分

示例2：B2下单购买5件（订单金额500元）
→ A获得直返：500 × 20% = 100积分（直返按单算，不管件数）

示例3：B3下单购买2件（订单金额200元）
→ A获得直返：200 × 60% = 120积分（直返按单算，不管件数）
```


## 6. 间返规则（固定数额制 · 按件计算）

### 6.1 间返标准

| 项目 | 标准值 | 说明 |
|------|--------|------|
| 间返固定数额 | **30积分/件** | 每件产品产生30积分间返 |
| 间返计算基础 | 按件数计算 | 与产品单价无关 |
| 间返对象 | 非直接发展人的层级节点 | 直接发展人已通过直返获取收益 |

### 6.2 间返核心分配逻辑

**核心原则**：从当前用户往上逐层检查，每件产品产生一笔30积分的间返，分配给 **最近的非直接发展人层级节点**。如果某层级节点恰好是当前用户的直接发展人，则 **跳过该层级**，间返继续向上分配给更上一层的非直接发展人节点。

**分配规则：**

1. 直返（百分比）始终返给 **直接发展人**
2. 间返（固定30积分/件）从当前用户往上逐层寻找 **非直接发展人** 节点进行分配
3. 每件产品对应一层间返，逐件向上分配
4. 遇到直接发展人所在层级时，跳过该层级，间返继续向上
5. 到达顶层节点或件数用完时停止

### 6.3 间返分配逻辑流程

```
用户下单N件产品：

第1件 → 检查上一层：
  ├─ 上一层 = 直接发展人？ → 跳过，间返给往上第2层（30积分）
  └─ 上一层 ≠ 直接发展人？ → 间返给上一层（30积分）

第2件 → 从第1件分配到的层级继续往上：
  ├─ 该层 = 直接发展人？ → 跳过，间返继续向上
  └─ 该层 ≠ 直接发展人？ → 间返给该层（30积分）

第3件、第4件……以此类推，直到顶层或件数用完
```

### 6.4 间返完整计算示例

#### 示例1：上一层就是直接发展人

```
场景：
- A发展了B（B是A的第1位直属下级，直返比例20%）
- B下单购买3件产品（每件100元，订单总金额300元）

返积分分配：
┌─────────┬──────────────────┬──────────────────────┬──────────┐
│ 产品件数 │ 返积分类型       │ 返给谁               │ 返积分金额│
├─────────┼──────────────────┼──────────────────────┼──────────┤
│ 整单     │ 直返（20%）      │ A（直接发展人）      │ 60积分   │
│ 第1件    │ 间返（固定30）   │ A的上一层（跳过A）   │ 30积分   │
│ 第2件    │ 间返（固定30）   │ 再往上一层           │ 30积分   │
│ 第3件    │ 间返（固定30）   │ 再往上一层           │ 30积分   │
└─────────┴──────────────────┴──────────────────────┴──────────┘

说明：B的上一层就是A（直接发展人），A已通过直返获得60积分，
      因此第1件的间返跳过A，分配给A的上一层。
```

#### 示例2：上一层不是直接发展人（挂靠关系）

```
场景：
- A发展了B（B是A的第3位直属下级，直返比例60%）
- B发展了C，但C挂靠在D名下（D不是C的直接发展人，B才是）
- 挂靠结构：D（上一层）→ C（当前用户）
- 实际发展关系：B → C

层级结构（从C往上）：
  第3层：A
  第2层：B（C的直接发展人）
  第1层：D（C的挂靠上级，非直接发展人）
  第0层：C（下单用户）

C下单购买4件产品（每件100元，订单总金额400元）：

返积分分配：
┌─────────┬──────────────────┬──────────────────────┬──────────┐
│ 产品件数 │ 返积分类型       │ 返给谁               │ 返积分金额│
├─────────┼──────────────────┼──────────────────────┼──────────┤
│ 整单     │ 直返（60%）      │ B（直接发展人）      │ 240积分  │
│ 第1件    │ 间返（固定30）   │ D（上一层，非发展人）│ 30积分   │
│ 第2件    │ 间返（固定30）   │ 跳过B → 分配给A     │ 30积分   │
│ 第3件    │ 间返（固定30）   │ A的上一层            │ 30积分   │
│ 第4件    │ 间返（固定30）   │ 再往上一层           │ 30积分   │
└─────────┴──────────────────┴──────────────────────┴──────────┘

说明：
- 直返始终给B（直接发展人），B获得400 × 60% = 240积分
- 第1件间返给D（上一层，非直接发展人），D获得30积分
- 第2件继续往上找，遇到B（直接发展人），跳过B，分配给A，A获得30积分
- 第3件继续往上，给A的上一层
- 第4件继续往上，给再上一层
```

#### 示例3：复杂挂靠场景

```
场景：
- A发展了B（B是A的第2位直属下级，直返比例20%）
- B发展了C
- C挂靠在D名下，D挂靠在B名下
- 挂靠结构：B → D → C
- 实际发展关系：B → C（B是C的直接发展人）

层级结构（从C往上）：
  第3层：A
  第2层：B（C的直接发展人，也是D的挂靠上级）
  第1层：D（C的挂靠上级，非直接发展人）
  第0层：C（下单用户）

C下单购买5件产品（每件100元，订单总金额500元）：

返积分分配：
┌─────────┬──────────────────┬──────────────────────┬──────────┐
│ 产品件数 │ 返积分类型       │ 返给谁               │ 返积分金额│
├─────────┼──────────────────┼──────────────────────┼──────────┤
│ 整单     │ 直返（20%）      │ B（直接发展人）      │ 100积分  │
│ 第1件    │ 间返（固定30）   │ D（上一层，非发展人）│ 30积分   │
│ 第2件    │ 间返（固定30）   │ 跳过B → 分配给A     │ 30积分   │
│ 第3件    │ 间返（固定30）   │ A的上一层            │ 30积分   │
│ 第4件    │ 间返（固定30）   │ 再往上一层           │ 30积分   │
│ 第5件    │ 间返（固定30）   │ 再往上一层           │ 30积分   │
└─────────┴──────────────────┴──────────────────────┴──────────┘

说明：
- B作为直接发展人获得直返100积分，不参与间返分配
- 第1件间返给D（最近的上一层非直接发展人）
- 第2件间返遇到B（直接发展人），跳过，分配给A
- 后续件数继续向上分配
```


## 7. 特殊情况处理

| 场景 | 处理规则 |
|------|----------|
| 下单件数超过可返层级数 | 超出部分不再返积分，归入平台收益 |
| 返到顶层仍有多余件数 | 剩余件数对应的间返积分归入平台收益 |
| 连续多层都是直接发展人 | 跳过所有直接发展人层级，间返继续向上分配给最近的非直接发展人 |
| 退款/退货 | 对应订单的全部返积分收回（从相关推广者账户中扣减） |
| 部分退款 | 按退款比例收回对应返积分 |
| 同一层级存在多个挂名用户 | 按系统挂靠顺序依次分配 |
| 产品单价变化 | 间返固定数额不受影响，始终按30积分/件计算 |
| 产品组合购买 | 按总件数计算，每件均触发一层间返分配 |
| 直属下级注销/退出 | 其下级自动上移或由系统重新分配挂靠 |
| 返积分账户余额不足扣除 | 记录负余额，后续获得返积分时优先抵扣 |


## 8. 返积分配置参数

| 配置项 | 默认值 | 是否可调整 | 调整权限 |
|--------|--------|-----------|----------|
| 第1位直属下级直返比例 | 20% | ✅ 可调整 | 双方协商后由甲方配置 |
| 第2位直属下级直返比例 | 20% | ✅ 可调整 | 双方协商后由甲方配置 |
| 第3位直属下级直返比例 | 60% | ✅ 可调整 | 双方协商后由甲方配置 |
| 间返固定数额 | 30积分/件 | ✅ 可调整 | 双方协商后由甲方配置 |
| 直属下级上限 | 3位 | ❌ 固定 | 33复制模式核心参数 |
| 积分有效期 | 12个月 | ✅ 可调整 | 双方协商后由甲方配置 |
| 最低提现额度 | 100积分 | ✅ 可调整 | 双方协商后由甲方配置 |


## 9. 直返与间返关系总结

| 对比项 | 直返 | 间返 |
|--------|------|------|
| 计算基础 | **按订单（每单）** | **按件数（每件）** |
| 计算方式 | 百分比（20%/20%/60%） | 固定数额（30积分/件） |
| 返利对象 | **始终为直接发展人** | **非直接发展人的层级节点** |
| 是否受挂靠影响 | **否**，只看发展关系 | **是**，按挂靠层级逐层向上 |
| 遇到直接发展人层级 | 直接返给发展人 | **跳过**，继续向上分配给非发展人 |
| 一单多件时 | 只计算一次（按整单金额×比例） | 每件分别计算（每件对应一层） |


## 10. 技术实现要点

| 实现项 | 说明 |
|--------|------|
| 发展关系存储 | 独立记录每位用户的直接发展人，与挂靠关系分离 |
| 挂靠关系存储 | 独立记录层级结构，支持滑落动态调整 |
| 返积分计算引擎 | 订单支付成功后触发，实时计算直返与间返 |
| 跳过逻辑实现 | 间返计算时逐层遍历，判断是否等于直接发展人 |
| 退款处理 | 原路扣减对应返积分，支持部分退款按比例扣减 |
| 负余额处理 | 允许负余额存在，后续返积分优先抵扣 |
| 数据审计 | 所有返积分记录可追溯，支持对账 |


> **本文档为《膳食类产品线上推广平台合作方案》附件二，与主方案具有同等约束力。**
>
> 版本：v0.0.1（Draft）
> 编制人：张牧之
> 编制日期：2026年